# Offline map package

Đặt file MBTiles tên `vkstreetfood.mbtiles` vào thư mục này để mobile app tải nền bản đồ offline.

Bạn cũng có thể upload qua API:

- `POST /api/offline/map-package` (multipart form field: `file`)
- `GET /api/offline/map-status`
- `GET /api/offline/map-package`
